version: 0.2

env:
  variables:
    SERVER_URL: 'https://server.solutionist.site/'

phases:
  pre_build:
    commands:
      - cd client
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: client/build
